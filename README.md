A simple CLI program to access the Flight Aware API.

### docker usage

```bash
docker run --rm -it siteworxpro/flightaware-cli:latest -l [username] -k [apikey] -a tail-owner -p '{"ident": "N12345"}'
```